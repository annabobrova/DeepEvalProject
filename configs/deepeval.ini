[deepeval]
# Model used by DeepEval as the "judge" LLM for metrics like GEval and Faithfulness.
# Requires OPENAI_API_KEY. You can also set this to an Anthropic model via a custom judge.
model = gpt-4o
